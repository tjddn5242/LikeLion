# 체중 관리 프로그램

weight = float(input("몸무게(kg)를 입력하세요: "))
tall = float(input("키(cm)를 입력하세요: ")) / 100

BMI = weight / tall ** 2

if BMI >= 20.0 and BMI < 25.0:
    print("표준입니다")
else:
    print("체중관리가 필요합니다")
